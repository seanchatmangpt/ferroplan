//! @generated by ggen from `.ggen/ferroplan-v26.8.1/ontology.ttl`.
//!
//! This template owns enum declarations only. Runtime structs remain in the
//! checked-in generated projection until grouped-result helpers are available.
use serde::{Deserialize, Serialize};
#[cfg(feature = "schema")]
use schemars::JsonSchema;

{% set current = "" %}
{% for row in results %}
{% if row.enum_name != current %}
{% if current != "" %}
}
{% endif %}
#[derive(Serialize, Deserialize, Clone, Copy, Debug, PartialEq, Eq)]
#[cfg_attr(feature = "schema", derive(JsonSchema))]
pub enum {{ row.enum_name }} {
{% set_global current = row.enum_name %}
{% endif %}
    #[serde(rename = "{{ row.serde_name }}")]
    {{ row.variant_name }}{% if row.rust_type %}({{ row.rust_type }}){% endif %},
{% endfor %}
}
