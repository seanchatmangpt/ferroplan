---
name: self-host
description: Run the complete Chatman ecosystem loop with Ferroplan as its first managed repository world. Use for substantial work on Ferroplan itself or when the user requests dogfooding.
effort: max
---

Self-host Ferroplan for `$ARGUMENTS`.

Execute the closed loop without collapsing roles:

1. `/chatman-ecosystem:phase-change` — inspect current product state.
2. `/chatman-ecosystem:configure` — establish Claude configuration conformance.
3. `/chatman-ecosystem:observe` — produce the admitted RDF-shaped world and eight work surfaces.
4. `/chatman-ecosystem:allocate` — run CMCA and bind the allocation envelope.
5. `/chatman-ecosystem:plan` — retain or manufacture a persistent Ferroplan plan.
6. Advance to `actuation=manufacturing` with a valid receipt.
7. `/chatman-ecosystem:manufacture` — execute one reversible plan step.
8. Observe the resulting drift and feed it back into the Session.
9. `/chatman-ecosystem:validate` — exercise exact source, config, build, and plan claims.
10. `/chatman-ecosystem:admit` — bind the plan envelope and hook frontier.
11. `/chatman-ecosystem:audit` — compute maximum lawful standing.
12. Continue until the goal is met, bounded search refuses, or evidence establishes a typed refusal.

Publication never enters this loop uninvited — only when the user
separately calls `/chatman-ecosystem:publish` or asks for it directly.
