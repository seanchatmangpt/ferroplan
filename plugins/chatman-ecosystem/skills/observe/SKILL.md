---
name: observe
description: Convert the current repository and hook frontier into a bounded RDF-shaped observation with explicit uncertainty and eight CMCA work surfaces. Use after mutation, failure, resume, or external drift.
context: fork
agent: chatman-ecosystem:rdf-observer
effort: high
---

Observe `$ARGUMENTS` without editing.

- Read the pending hook ledger and active phase vector.
- Inspect exact repository state, changed paths, manifests, dependencies, generated ownership, commands, failures, and available evidence.
- Project PROV-O/OCEL/QUDT/SHACL-shaped claims using stable identifiers.
- Produce exactly eight CMCA candidates in the canonical factor order.
- Separate observed facts, projection laws, uncertainty, and refusals.
- Return machine-usable JSON plus the maximum lawful epistemic standing.

This is eyes-only work. No calling CMCA, no planning, no editing, no
authorizing actuation.
