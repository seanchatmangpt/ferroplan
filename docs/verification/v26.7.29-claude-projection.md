# v26.7.29 Claude Projection Verification

This checklist separates source materialization from exact-head runtime standing.

## Source projection

- [ ] Every JSON artifact parses.
- [ ] Every agent and skill frontmatter block loads.
- [ ] The main plugin manifest has version `26.7.29`.
- [ ] The main plugin is opt-in with `defaultEnabled: false`.
- [ ] The main plugin declares no `lspServers` and contains no `.lsp.json`.
- [ ] The standalone config-LSP marketplace entry remains explicit.
- [ ] All thirteen skills are present.
- [ ] All eight agents are present.
- [ ] Every non-manufacturing agent denies `Write`, `Edit`, and `NotebookEdit`.
- [ ] Only `source-manufacturer` is active in `actuation=manufacturing`.
- [ ] `source-manufacturer` declares `isolation: worktree`.
- [ ] Both monitors use `on-skill-invoke` activation.
- [ ] PostToolUse mutation hooks do not directly advance phase state.
- [ ] PostToolBatch, ConfigChange, WorktreeCreate, and WorktreeRemove manufacture bounded lifecycle candidates.
- [ ] PreToolUse manufactures a structured actuation intent.
- [ ] Artifact ownership covers every generated Claude projection surface.
- [ ] `validate-claude-projection.py` returns `valid: true`.

## Loader and installation

- [ ] `claude plugin validate plugins/chatman-ecosystem --strict` passes.
- [ ] Marketplace validation passes.
- [ ] Clean marketplace add succeeds.
- [ ] Clean project-scope plugin install succeeds.
- [ ] `/plugin` reports no load errors.
- [ ] `/agents` shows the eight namespaced agents.
- [ ] `/skills` shows the thirteen namespaced skills.
- [ ] User configuration resolves from a supported settings source.
- [ ] Plugin cache version resolves to `26.7.29` rather than stale commit-SHA material.

## MCP protocol

- [ ] The single `ferroplan` stdio process initializes.
- [ ] `tools/list` returns all sixteen tools.
- [ ] `resources/list` succeeds.
- [ ] Stateless parse succeeds.
- [ ] Stateless solve succeeds.
- [ ] Stateless validate succeeds.
- [ ] Decompose succeeds.
- [ ] Session open/status/observe/think/close succeeds.
- [ ] CMCA allocation succeeds for exactly eight nodes.
- [ ] Canonical digest succeeds.
- [ ] Allocation envelope binds and verifies.
- [ ] Plan envelope binds and verifies.
- [ ] Tampered payload and receipt are refused.

## Effective phase and event sourcing

- [ ] With no pending events, effective vector equals canonical vector.
- [ ] With pending events, effective vector collapses to observed/unallocated/unplanned/sealed/drifted/unknown.
- [ ] The canonical snapshot remains unchanged during effective projection.
- [ ] Admission closes the exact event frontier.
- [ ] A stale snapshot cannot make protected actuation publishable.
- [ ] Parallel tool calls produce exact per-tool events and one batch summary.
- [ ] Config changes and worktree lifecycle events are retained as candidates.
- [ ] Replay detects missing, duplicated, reordered, and forked receipt heads.

## Recursive CMCA

- [ ] Every frontier contains exactly eight nodes.
- [ ] Every node contains exactly ten factors in canonical order.
- [ ] Parent relations are acyclic.
- [ ] Recursive descent binds the parent allocation receipt.
- [ ] Recursive descent admits a new local observation frontier.
- [ ] Recursive return binds a consequence to the parent node.
- [ ] No implementation path flattens recursive frontiers into an unbounded candidate array.

## Manufacture and validation

- [ ] The controller cannot directly edit.
- [ ] Observer, allocator, planner, validator, auditor, and config architect cannot directly edit.
- [ ] Source manufacture creates an isolated worktree.
- [ ] Ownership guard refuses projected output when no owner changed.
- [ ] Ownership guard permits owner-first regeneration attempts.
- [ ] Worktree mutation produces a pending observation frontier.
- [ ] Python compile and unit fixtures pass.
- [ ] Shell syntax checks pass.
- [ ] Cargo format passes.
- [ ] `cargo check -p ferroplan-mcp --bin ferroplan-mcp` passes.
- [ ] `cargo test -p ferroplan-mcp` passes.
- [ ] Exact changed source receives appropriate unit, integration, and end-to-end validation.
- [ ] VAL or another independent implementation validates PDDL semantics when required.
- [ ] Same-engine replay is labeled correctly and never promoted to semantic independence.

## Protected actuation

- [ ] A protected command without a grant is denied.
- [ ] Denial records an exact `ActuationIntent`.
- [ ] The intent binds operation, command digest, effective phase, pending count, predecessor receipt, and reversibility.
- [ ] Grant derivation refuses a stale, malformed, mismatched, or unverified receipt.
- [ ] Grant derivation refuses a non-publishable phase.
- [ ] Grant derivation refuses a pending frontier.
- [ ] A valid grant binds the exact intent and active receipt.
- [ ] A changed command requires a new intent and grant.
- [ ] An expired grant is refused.
- [ ] The compatibility regex fence remains active.
- [ ] Successful execution is recorded separately from grant admission.
- [ ] The resulting GitHub object is captured as execution evidence.

## Standing

The source patch may claim at most `PARTIAL_ALIVE` until every required runtime rail above is exercised against the exact branch head.

Use:

- `BUILD_BROKEN` for an exercised failing surface;
- `BLOCKED` for an admitted dependency preventing progress;
- `UNKNOWN` for unavailable evidence or executor;
- `UNSUPPORTED` for an unwired capability class;
- `ALIVE` only for the complete exact-head claim.
