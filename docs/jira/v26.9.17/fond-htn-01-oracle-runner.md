---
id: fond-htn-01-oracle-runner
type: oslc_cm:ChangeRequest
requirement: earl:TestRequirement
dcterms:title: "Formalize the koala oracle runner (flock-serialized, idempotent)"
standing: ALIVE
worktree: none (/tmp only)
created: 2026-09-17T21:30:00Z
---

Read `_FOND-HTN-WAVE-CONTEXT.md` first. You touch ONLY `/tmp/fond-oracle/` — no repo, no worktree.

## Scope
Deliver the runner contract from the context doc:
1. `/tmp/fond-oracle/build.sh` — flock `/tmp/fond-oracle/.lock`; idempotent; verifies the three binaries exist and are executable; if missing, rebuilds per the macOS notes in the context doc (brew bison/flex/gengetopt; `make CXX=g++ CC=gcc`; bliss unzip→patch→sed→`make CC=g++`). Exit 0 when ready; print a one-line READY manifest (binary → sha256).
2. `/tmp/fond-oracle/oracle-run.sh <domain> <problem> [--mode M] [--timeout SEC] [--heur H]` — takes the same flock (serializes `solve.py`'s shared temp files); runs `python3 solve.py ...` with `--output` into `/tmp/fond-oracle/runs/<sha-of-args>/policy.txt`; enforces timeout (solve.py takes minutes — convert; also wrap with an outer wall kill as backstop); classifies and prints exactly ONE JSON line: status ∈ SOLVED (policy artifact non-empty / planner success output), NOSOLUTION, PARSE_ERROR (parser stage failed), ERROR, TIMEOUT. Capture full logs to `runs/<id>/log.txt`. A NOSOLUTION verdict exits 0.
3. Smoke gates: Transport pfile01 `--mode flexible` → SOLVED; a deliberately broken problem file (truncate pfile01 to 3 lines) → PARSE_ERROR; `--mode fixed-ld --timeout 60` on Transport pfile01 → some verdict with exit 0. Run each smoke twice to prove flock serialization works (launch two in parallel with `&`, both must return consistent verdicts).
4. `README.md` in /tmp/fond-oracle: usage, JSON schema, artifact layout, known quirks.

## Gates
Three smokes green + double-run serialization check. No repo changes (verify `git -C /Users/sac/ferroplan status --porcelain` unchanged apart from pre-existing untracked).

## History
| ts | standing | branch+SHA | gates+exits | remaining |
|---|---|---|---|---|
| 2026-09-17T21:30:00Z | BLOCKED | none | — | all |
| 2026-09-17T21:35:10Z | PARTIAL_ALIVE | none (HEAD 2a09e48 baseline) | orient: 3 binaries present+executable (parser/grounder/planner, built 14:24–14:28Z); Transport corpus at /tmp/fond-review/domains/Transport (domain.hddl + pfile01–15); solve.py read: NO CLI timeout flag (30-min default unreachable from argv → --timeout SEC must be outer wall kill); failure markers = `failed to ground|serialize` on stderr; flags --flexible/--fixed-ld/--fixed --ff/--add/--max/--lmcut --output; /tmp/fond-oracle absent | build.sh, oracle-run.sh, 3 smokes ×2 parallel, README |
| 2026-09-17T21:42:00Z | PARTIAL_ALIVE | none | COLLISION: second T01 writer active in /tmp/fond-oracle (build.sh+oracle-run.sh appeared 21:38–39Z; live oracle-run procs on Transport pfile01–04 at 21:39–40Z: flexible-ff t180, fixed-ld-add t120, plus a shop3-loan-noplan probe). Their solver invocation is flock-clean (same .lock) so solves serialize; script files NOT edited by me while their runs are in-flight. Contract deltas observed: run-id=timestamp-pid (ticket: sha-of-args), log=run.log (ticket: log.txt), SOLVED gated on `Success probability` marker (ticket: non-empty artifact), PARSE_ERROR keyed on `failed to parse` only, build.sh verifies without parser/grounder rebuild and prints no READY sha manifest. Evidence harvested from their runs: SOLVED policy.txt = banner+plan+`Success probability: 4.0000` (non-empty ✓); parse-garbage path ends `failed to serialize`+KeyError traceback. Plan: wait drain → reconcile their scripts to ticket contract (EXTEND, not clobber) → run 3 doubled smokes | drain sibling procs; reconcile scripts; 3 smokes ×2 parallel; README; repo-status check |
| 2026-09-17T21:55:30Z | PARTIAL_ALIVE | none | RECONCILED via atomic rename (running instances kept old inode; consumers T02/T03 unaffected: JSON schema/exit codes preserved, run.log kept as hardlink of log.txt, old run dirs untouched). Classifier pinned on real logs: `Parse error` (ANSI-wrapped, truncated-pfile01 log) / `failed to parse|ground` → PARSE_ERROR; bare `failed to serialize`+Traceback on valid parse (loan-noplan log) → ERROR; rc124/`timeout for` → TIMEOUT; non-empty artifact w/o unsat markers → SOLVED; else NOSOLUTION. Also: sha-of-args run IDs, log.txt, orphan-planner pkill on rc124, usage/harness exits 1, verdicts exit 0. Reused sibling fixture smoke/truncated-pfile01.hddl (3 lines ✓). Removed stray oracle-lib.sh | gate0 build.sh; 3 smokes ×2 parallel; README; repo-status check |
| 2026-09-17T21:58:20Z | PARTIAL_ALIVE | none | GATE0 build.sh: PASS — one-line READY manifest (parser 27479d13…, grounder 2eb99145…, planner ba164cd8…), exit=0; flock waited 100s on in-flight sibling solve (serialization witnessed) | smokes 1–3 ×2 parallel; README; repo-status check |
| 2026-09-17T22:03:30Z | PARTIAL_ALIVE | none | GATE1 flexible pfile01 ×2 parallel: PASS — exits 0/0; both exactly-one valid JSON line (4-key schema); both SOLVED (consistent); same sha-of-args run dir e10fa729…/policy.txt (deterministic ID); pair elapsed 81s vs wall_s 0.224/0.260 → flock serialization witnessed (queued behind sibling solves) | smoke 2 (PARSE_ERROR ×2), smoke 3 (fixed-ld t60 ×2); README; repo-status check |
| 2026-09-17T22:19:30Z | PARTIAL_ALIVE | none | GATE2 (pre-fix runner) revealed DEFECT CLASS, not runner bug: both runs TIMEOUT @300s wall. Forensics: fixture intact (3 lines); run log = AO* progress to depth 1335 (no parser banner) + stale planner/result.json (mtime live) → solve.py reached SEARCH on a STALE result.json left by sibling timeout-killed loan-noplan runs (solve.py removes shared temps only on clean paths; loan-noplan searches never terminate). Root cause is solve.py shared-temp design (wave-context hazard note), surfacing across runs. FIX (in runner, under flock): pre-clean parsed.htn/result.sas+/result.json before solve.py; on rc124 post-kill pkill planner (pattern dropped leading path: /tmp→/private/tmp in argv) + post-clean temps. Swapped atomically 22:16:54. Gates 1–3 to be rerun against FINAL runner (証: gates certify the delivered artifact; gate-1 pass predates fix) | rerun gates 1–3 ×2 parallel on final runner; README; repo-status check |
| 2026-09-17T22:22:40Z | ALIVE | none (/tmp only; repo HEAD 2a09e48 untouched) | GATES on FINAL runner, all doubled parallel (bash /tmp/fond-oracle/smoke-gates.sh, exit 0): G1 flexible pfile01 → exits 0/0, SOLVED/SOLVED consistent, wall 0.188/0.133s, pair 23s; G2 truncated fixture t30 → exits 0/0, PARSE_ERROR/PARSE_ERROR consistent (log evidence: `Parse error … in line 4` in runs/79bb6aa0…/log.txt), pair 61s; G3 fixed-ld t60 pfile01 → exits 0/0, TIMEOUT/TIMEOUT consistent at wall 60.30/60.52s (some-verdict ✓; real datum: fixed-ld does not solve pfile01 in 60s where flexible solves in 0.13s), pair 187s. Each JSON = exactly one 4-key line (validated). Flock serialization witnessed throughout (pairs + sibling sweep interleaved, zero corruption, verdicts consistent). REPO GATE: `git status --porcelain` = my ticket file only (+6 History rows) vs baseline; other M tickets (02/03/04/05/07/08/09/10) are sibling agents' rows; untracked plugins/* pre-existing. Never pushed, nothing else touched | README ✓, smoke-gates.sh ✓ — final report |
| 2026-09-17T22:23:30Z | ALIVE | none (/tmp only) | DELIVERED: /tmp/fond-oracle/{build.sh, oracle-run.sh, README.md, smoke-gates.sh, smoke/, runs/}. Standing ALIVE: build.sh exit 0 with READY sha256 manifest; oracle-run.sh contract-complete (sha-of-args IDs, log.txt+run.log hardlink, 5-status classifier evidence-pinned, verdicts exit 0, flock + stale-state pre/post clean + orphan pkill). 比 note: runner skeleton originated from a duplicate T01 dispatch mid-flight; reconciled/extended to ticket contract by this agent (atomic renames, zero clobber of live consumers); collision + forensics ledgered above. Operator wrote zero bytes. Remaining for consumers: none — runner is consumable; golden-JSON harvest is T02/T03 territory | end |
