;; hand-authored adversarial fixture (ticket fond-htn-14) — not derived from any corpus.
;; Unicode identifiers (CJK + Greek) in predicates/tasks/objects — positive
;; control: must parse/ground without panic.
(define (domain unicode-ids)
  (:predicates (在 ?位置) (άνοιγμα ?x))
  (:action 移动
    :parameters (?位置)
    :precondition ()
    :effect (在 ?位置))
  (:task 运输
    :parameters (?位置)))
