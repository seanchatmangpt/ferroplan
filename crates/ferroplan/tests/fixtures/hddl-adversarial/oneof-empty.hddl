;; hand-authored adversarial fixture (ticket fond-htn-14) — not derived from any corpus.
;; '(oneof)' with zero branches — not a meaningful non-deterministic choice.
(define (domain oneof-empty)
  (:predicates (at ?x))
  (:action move
    :parameters (?x)
    :precondition ()
    :effect (oneof)))
