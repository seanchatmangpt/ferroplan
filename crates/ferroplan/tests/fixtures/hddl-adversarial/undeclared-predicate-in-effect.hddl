;; hand-authored adversarial fixture (ticket fond-htn-14) — not derived from any corpus.
;; Effect uses predicate 'teleport', never declared in ':predicates'.
(define (domain undeclared-pred)
  (:predicates (at ?x))
  (:action move
    :parameters (?x)
    :precondition ()
    :effect (teleport ?x)))
