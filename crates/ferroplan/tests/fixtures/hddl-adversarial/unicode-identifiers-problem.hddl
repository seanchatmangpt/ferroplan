;; hand-authored adversarial fixture (ticket fond-htn-14) — not derived from any corpus.
;; Problem half of the unicode positive control.
(define (problem unicode-p1)
  (:domain unicode-ids)
  (:objects θ1)
  (:init)
  (:goal (在 θ1))
  (:htn :ordered-subtasks (移动 θ1)))
