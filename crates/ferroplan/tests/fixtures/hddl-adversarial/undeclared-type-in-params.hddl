;; hand-authored adversarial fixture (ticket fond-htn-14) — not derived from any corpus.
;; Parameter type 'vehicle' never declared in ':types'.
(define (domain undeclared-type)
  (:predicates (at ?x))
  (:action move
    :parameters (?x - vehicle)
    :precondition ()
    :effect (at ?x)))
