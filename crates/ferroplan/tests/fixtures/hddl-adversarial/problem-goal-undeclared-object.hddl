;; hand-authored adversarial fixture (ticket fond-htn-14) — not derived from any corpus.
;; ':goal' references object 'ghost', never declared in ':objects'.
(define (problem ghost-p1)
  (:domain ghost-domain)
  (:objects x1)
  (:init (at x1))
  (:goal (at ghost))
  (:htn :ordered-subtasks (move x1)))
