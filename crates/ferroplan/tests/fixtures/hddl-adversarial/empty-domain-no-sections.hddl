(define (domain empty))
;; hand-authored adversarial fixture (ticket fond-htn-14) — not derived from any corpus.
;; Structurally legal but sectionless domain: asserted as accepted-no-panic (tolerance contract).
